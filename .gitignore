# Datasets — download separately (see README)
data/

# Trained model weights and kernel matrices
models/*.pt
models/*.joblib
models/*.npy

# Large numpy arrays
results/*.npz

# Python cache
__pycache__/
*.pyc
*.pyo

# Logs
run_log.txt
results/run_log.txt

# Environment
.env
*.egg-info/
dist/
build/

# Submission package — regenerable copies/binaries (rebuild via scripts/build_submission.py).
# Authored text (manifest, cover letter, data availability) IS tracked.
submission/figures/
submission/tables/
submission/manuscript/
submission/supplementary/
submission/code_archive/
